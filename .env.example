# Copie este arquivo para .env.local e cole sua chave.
# Pegue a sua em https://web3forms.com/ (é grátis, chave na hora, sem confirmação).
# .env.local já está no .gitignore (via *.local) — nunca comite a chave de verdade.
VITE_WEB3FORMS_ACCESS_KEY=
